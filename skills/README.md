# Skills README

`skills/` 目录的主入口已改为 [[index]]。

如果你是在 Obsidian 或文件浏览器里先打开了这个 `README.md`，请优先跳转到：

- [[index]]

`index.md` 中包含：

- `skills/` 目录结构说明
- 每个 skill 的简要总结
- `skills/` 与 `.claude/skills/` 的路径对应关系

真实生效的位置仍然是：

- `.claude/skills/`
